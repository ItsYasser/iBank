class Transaction {
  final String id;
  final String title;
  final double amount;
  final DateTime date;
  final Map category;
  Transaction({this.id, this.title, this.amount, this.date, this.category});
}
