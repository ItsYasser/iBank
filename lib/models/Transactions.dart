import 'dart:ui';

class Transaction {
  final String id;
  final String title;
  final double amount;
  final DateTime date;
  final Color color;

  Transaction({this.id, this.title, this.amount, this.date, this.color});
}
